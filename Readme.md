## Go Microservice Example with Fiber Go


Run the sever first

`go run main.go`

Check microservices response over http, start the client server


`go run client/client.go`


Check the response using `curl` or open `http://localhost:3000` into the browser
